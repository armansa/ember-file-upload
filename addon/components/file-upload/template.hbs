<input
  id={{for}}
  type="file"
  accept={{accept}}
  capture={{capture}}
  multiple={{multiple}}
  disabled={{disabled}}
  onchange={{action "selected" value="target.files"}}
  hidden
>

{{~yield queue~}}
